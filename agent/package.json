{
  "name": "dump-agent",
  "version": "1.0.0",
  "description": "Frida agent written in TypeScript",
  "private": true,
  "main": "agent/index.ts",
  "type": "module",
  "scripts": {
    "prepare": "npm run build",
    "build": "frida-compile agent/index.ts -o dump.js -c",
    "watch": "frida-compile agent/index.ts -o dump.js -w"
  },
  "devDependencies": {
    "@types/frida-gum": "^19.0.0",
    "@types/node": "^18.14.0"
  },
  "dependencies": {
    "frida-objc-bridge": "^8.0.5"
  }
}
