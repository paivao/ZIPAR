{
  "name": "dump-agent",
  "version": "1.0.0",
  "description": "Frida agent written in TypeScript",
  "private": true,
  "main": "agent/index.ts",
  "type": "module",
  "scripts": {
    "prepare": "npm run build",
    "build": "frida-compile src/main.ts -o ../src/zipar/agent.js -T none",
    "watch": "frida-compile src/main.ts -o ../src/zipar/agent.js -w",
    "lint": "tslint -c tslint.json 'src/**/*.ts'"
  },
  "devDependencies": {
    "@types/frida-gum": "^19.0.0",
    "@types/node": "^18.14.0"
  },
  "dependencies": {
    "frida-objc-bridge": "^8.0.5"
  }
}
